# API

Referencia de la API REST + WebSocket del backend.

!!! info "Estado"
    Vacío. Se autogenera desde OpenAPI en **M3** ([Roadmap](../ROADMAP.md)).

## Contenido previsto

- Autenticación (JWT, refresh rotation)
- Endpoints por recurso (organizations, users, devices, telemetry, alerts, calibration, OTA)
- WebSocket `/devices/{id}/live`
- Webhooks (M5)
- Rate limits
- Códigos de error
- Ejemplos con `curl` y con clientes Python / TypeScript
- Versionado de la API y política de breaking changes

OpenAPI en vivo del backend: `https://api.acuaponicoduino.com/docs` (cuando esté desplegado).
